<nav class="navbar fixed-top navbar-expand-lg navbar-light bg-light shadow-lg">
    <a class="navbar-brand" href="{{ path('post_index') }}">
        <img src="{{ asset('resource/clone-trooper_icon.png') }}" height="32" alt="clone-trooper icon">
        <p class="h3 mb-0 ml-2 d-lg-none d-xl-none">Clonest</p>
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbar" aria-controls="navbar" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbar">

        <form class="my-2 my-lg-0 w-100 px-0 px-md-3">

            <label for="search-bar" class="sr-only">Search</label>
            <div class="input-group">
                <div class="input-group-prepend">
                    <button id="search-bar-button" class="btn btn-outline-primary text-black-50" type="submit">
                        <i class="fas fa-search"></i>
                    </button>
                </div>
                <input class="form-control mr-sm-2" type="text" id="search-bar" placeholder="Search">
            </div>
        </form>

        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="btn btn-outline-light text-black-50" href="{{ path('post_index') }}">
                    <i class="fas fa-home"></i>
                </a>
            </li>

            {% if app.user %}
                <li class="nav-item">
                    <a class="btn btn-outline-light text-black-50" href="{{ path('user_index') }}">
                        <i class="fas fa-user"></i>
                    </a>
                </li>

                <li class="nav-item">
                    <a class="btn btn-danger" href="{{ path('app_logout') }}">
                        <i class="fas fa-sign-out-alt"></i>
                    </a>
                </li>
            {% endif %}

            {% if not app.user %}
                <li class="nav-item">
                    <a class="btn btn-primary" href="{{ path('app_login') }}">
                        <i class="fas fa-sign-in-alt"></i>
                    </a>
                </li>

                <li class="nav-item">
                    <a class="btn btn-outline-primary" href="{{ path('app_register') }}">
                        <i class="fas fa-user-plus"></i>
                    </a>
                </li>
            {% endif %}

        </ul>
    </div>
</nav>
